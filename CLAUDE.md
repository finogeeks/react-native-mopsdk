- 所有的回答请使用中文
- 请详细了解整个项目中的内容，包括example里的内容
- 可以将了解到的内容存储到项目根目录下的document文件夹中，如果不存在这个文件夹，则可以先创建这个文件夹
- 这次的任务是升级RN环境，RN的版本需要支持Xcode 16 和 Xcode 26
- 执行过程中请创建todolist，防止你忘记上下文内容
- 升级过程完成后，请自行运行编译iOS & Android，确保正常运行，若编译报错，请自行修复。
- 任何产生的临时文档请放在根目录下的tempDocument文件夹，如果没有这个文件夹，请自行创建这个文件夹
- 请不要做任何git 操作
- 如果涉及到网络的任务，可以自己开代理 export https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890 all_proxy=socks5://127.0.0.1:7890
- 这次任务请先解决好升级后iOS可以正常编译,请先不用管Android
- 请注意,不要删除和注释掉Podfile中的   use_flipper!()